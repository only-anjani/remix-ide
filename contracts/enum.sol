// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract enumSample{

    //enum/enumeration = it is like a dropdown and have to select only from the set parameters. 
    enum orderStatus{orderReceived, packaged, shipped, received}
}